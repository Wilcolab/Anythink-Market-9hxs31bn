/**
 * Converts a string to camelCase.
 * The function removes spaces, hyphens, and underscores, and capitalizes each word except the first.
 * Example:
 *   toCamelCase('hello_world-example string') // returns 'helloWorldExampleString'
 *
 * @param {string} str - The input string to convert.
 * @returns {string} The camelCase version of the input string.
 */